# Inicializar proyecto

npm init -y
npm i nodemon -D -E
sacar ^ del json
en el json cambiar el "type" a "type=module"

"dev": "nodemon app/main.js"

# instalar express
npm i express -e

# instalar mongodb
npm i mongo db
